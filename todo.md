# Задачи по темам оформления

## Проблема
- Тёмная тема слишком тёмная на фото/изображениях — не видно контент
- Нужно отрегулировать работу всех тем оформления

## Темы для реализации
1. **light** — светлая тема ✅ (реализована)
2. **dark** — тёмная тема ✅ (реализована, нужна корректировка)
3. **blue** — синяя тема ❌
4. **purple** — фиолетовая тема ❌
5. **ambient** — атмосферная тема ❌
6. **relax** — расслабляющая тема ❌

## Что нужно сделать

### 1. Исправить тёмную тему (приоритет)
- [ ] Увеличить `--background` в `.dark` с `oklch(0.145 0 0)` до `oklch(0.2 0 0)` или светлее
- [ ] Увеличить `--card` с `oklch(0.205 0 0)` до `oklch(0.25 0 0)`
- [ ] Настроить `--muted` для лучшей читаемости
- [ ] Проверить отображение 3D книги в тёмной теме

### 2. Освещение в 3D сцене
Файл: `src/components/book/Lighting.tsx`
- [ ] Добавить адаптивное освещение в зависимости от темы
- [ ] Для тёмной темы увеличить `ambientLight` с `0.15` до `0.3-0.4`
- [ ] Добавить более яркие акцентные источники света

### 3. Реализовать переключатель тем
Файл: `src/components/ui/SettingsBar.tsx`
- [ ] Изменить с 2 состояний (dark/light) на 6 тем
- [ ] Добавить UI для выбора темы (dropdown или цикл)

### 4. Добавить CSS переменные для новых тем
Файл: `src/app/globals.css`
- [ ] `body.blue-theme` — синяя тема
- [ ] `body.purple-theme` — фиолетовая тема
- [ ] `body.ambient-theme` — атмосферная тема
- [ ] `body.relax-theme` — расслабляющая тема

### 5. Логика переключения тем
Файл: `src/app/page.tsx`
- [ ] Изменить `useState<"dark" | "light">` на `useState<string>`
- [ ] Обновить `getInitialTheme()` для поддержки 6 тем
- [ ] Обновить `useEffect` для применения классов тем

### 6. Тестирование
- [ ] Проверить все темы на разных компонентах
- [ ] Убедиться, что 3D книга видна во всех темах
- [ ] Проверить читаемость текста
